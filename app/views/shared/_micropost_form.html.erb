<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: t(:post_placeholder) %>
  </div>
  <%= f.submit t(:post_form), class: "btn btn-large btn-primary" %>
<% end %>